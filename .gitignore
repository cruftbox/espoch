# ESP-IDF build output
build/
sdkconfig
sdkconfig.old

# Auto-downloaded managed components — reproduced from dependencies.lock on build.
# (The patched Waveshare BSP is vendored under components/ and IS tracked.)
managed_components/

# VS Code
.vscode/

# macOS
.DS_Store

# WiFi credentials — keep the password off GitHub. Use wifi_secrets.h.example.
main/wifi_secrets.h

# Personal, machine-specific Claude Code permissions (absolute install paths).
.claude/settings.local.json
